sqlite> CREATE TRIGGER create_userprofile_after_user_insert
AFTER INSERT ON users
FOR EACH ROW
BEGIN
  INSERT INTO userprofiles (userid, activepictureurl, firstname, lastname, fullname)
  VALUES (
    NEW.id,
    NEW.activepictureurl,
    NEW.firstname,
    NEW.lastname,
    NEW.fullname
  );
END;
